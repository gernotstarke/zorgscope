# Local development runs the same image Fly runs. The process is stateless, so there is
# nothing else to bring up: no database, no volume.
services:
  app:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - ../.env
    environment:
      PORT: "8080"
    ports:
      - "8080:8080"
